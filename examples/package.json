{
  "name": "examples",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Uses the svgmotion package the way a build script would.",
  "scripts": {
    "generate": "node generate.mjs"
  },
  "dependencies": {
    "jsdom": "^28.0.0",
    "svgmotion": "workspace:*"
  }
}
